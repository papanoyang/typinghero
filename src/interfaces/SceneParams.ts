import SceneKeys from "../constants/SceneKeys";

interface SceneParams {
    startWidthFadeIn: boolean;
    stage: number;
    isClear: boolean;
    prevScene: SceneKeys;
};
export default SceneParams;